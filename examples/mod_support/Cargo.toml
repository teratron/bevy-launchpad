[package]
name = "example_mod_support"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bevy = { workspace = true, default-features = true }
bevy_launchpad = { workspace = true, features = ["ui"], default-features = true }
