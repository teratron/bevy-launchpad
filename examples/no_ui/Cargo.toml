[package]
name = "example_no_ui"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bevy = { version = "0.18", default-features = false }
bevy_launchpad = { path = "../..", default-features = false }
