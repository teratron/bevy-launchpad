[package]
name = "example_custom_settings"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bevy = { version = "0.18" }
bevy_launchpad = { path = "../..", features = ["ui"] }
ron = "0.12"
serde = { version = "1.0", features = ["derive"] }
