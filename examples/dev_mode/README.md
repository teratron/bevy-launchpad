# Ассеты для запуска примера `dev_mode`

Для корректной работы этого примера (в обычном режиме) необходимо вручную добавить следующий файл:

- `examples/dev_mode/assets/branding/studio.png` — Логотип студии

При запуске с флагом `--skip-splash` этот файл не потребуется.
