[package]
name = "example_multi_instance"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bevy = { workspace = true, default-features = true }
bevy-launchpad = { workspace = true, features = ["ui"], default-features = true }
